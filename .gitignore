# dependencies
node_modules/

# build output
app/dist/
dist/

# env / secrets
.env
*.env.local
.auth-secret

# real user data (names/emails/avatars) — never belongs in the public repo
server/data/users.json
server/data/progress.json

# OS
.DS_Store

# logs
*.log

# personal study material & design references — not app code, kept out of the public repo
/notes/
learning_ux.png
/_preview_mesh_analysis.html
